Something is wrong with PR $ARGUMENTS

Take a look at the PR, examine any merge conflicts or test failures. Investigate and correct them. Always fix the merge conflicts first. Always lint your changes. Push the corrected code to the branch in the PR, and then examine the test run to ensure the tests all pass, and there are no merge conflicts. If they do not, continue this pattern until they do.
