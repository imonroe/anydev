You are in a Drupal 10 website, which uses Lando for a local development environment.
The installation profile lives in the `docroot/profiles/custom/stanford_profile` directory.
The theme is in `docroot/profiles/custom/stanford_profile/themes/stanford_basic` directory.
Remember to run drush, composer, and so forth by appending `lando` at the beginning of the command in order to run the 
command in the container, not in the host.
This particular Drupal site uses JSON API for importing information from an external FileMaker database.

